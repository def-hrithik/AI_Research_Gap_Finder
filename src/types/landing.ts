export interface LandingExample {
  id: string;
  topic: string;
  teaser: string;
  coverColor: string;
  gapId: string;
}

export interface Testimonial {
  id: string;
  name: string;
  role: string;
  quote: string;
}

export interface FaqItem {
  question: string;
  answer: string;
}